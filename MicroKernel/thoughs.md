About accelerating MicroKernel in aarch64。Can run kernel and microkernel server in higher half kernel, when pass much data to server, just change the root page table for higher half kernel(TTBR1 )。

    